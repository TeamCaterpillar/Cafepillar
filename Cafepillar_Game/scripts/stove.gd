extends Control

@export var slot1 : TextureRect
@export var slot2 : TextureRect
# add however many slots are needed, can dynmaically size the slots based on recipe size

var slot1_resource : Resource
var slot2_resource : Resource

# Add cooking logic below
